<Window
    AllowsTransparency="True"
    Background="Transparent"
    Height="420"
    ResizeMode="NoResize"
    Title="Сообщение"
    Width="520"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    x:Class="Cellverse.View.CustomMessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border BorderThickness="0">
        <Border
            Background="{StaticResource BgDark}"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1"
            CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="44" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Title Bar  -->
                <Border
                    Background="{StaticResource BgPanel}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="12,12,0,0"
                    Grid.Row="0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <!--  Иконка и заголовок  -->
                        <StackPanel
                            HorizontalAlignment="Center"
                            Margin="16,0,0,0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                            <Border
                                Background="{StaticResource AccentBlue}"
                                CornerRadius="6"
                                Height="24"
                                Margin="0,0,12,0"
                                Width="24"
                                x:Name="TitleIconBorder">
                                <TextBlock
                                    FontSize="14"
                                    HorizontalAlignment="Center"
                                    Text="💬"
                                    VerticalAlignment="Center"
                                    x:Name="TitleIconText" />
                            </Border>
                            <TextBlock
                                FontSize="15"
                                FontWeight="SemiBold"
                                Foreground="{StaticResource TextPrimary}"
                                Text="Сообщение"
                                VerticalAlignment="Center"
                                x:Name="TitleText" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!--  Основной контент  -->
                <Grid Grid.Row="1" Margin="32,24,32,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  Большая иконка  -->
                    <Border
                        Background="{StaticResource BgCard}"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="50"
                        Grid.Row="0"
                        Height="100"
                        HorizontalAlignment="Center"
                        Width="100"
                        x:Name="IconBorder">
                        <TextBlock
                            FontSize="64"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            x:Name="IconText">
                            ✓
                        </TextBlock>
                    </Border>

                    <!--  Текст сообщения в карточке  -->
                    <Border
                        Background="{StaticResource BgCard}"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Grid.Row="2"
                        Padding="20,16">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <TextBlock
                                FontSize="14"
                                Foreground="{StaticResource TextPrimary}"
                                TextAlignment="Center"
                                TextWrapping="Wrap"
                                x:Name="MessageTextBlock">
                                Ваше сообщение здесь
                            </TextBlock>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!--  Кнопка  -->
                <Border
                    Background="{StaticResource BgPanel}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12"
                    Grid.Row="2"
                    Padding="32,16,32,20">
                    <Button
                        Click="OkButton_Click"
                        Content="OK"
                        Height="48"
                        Style="{StaticResource BtnPrimary}"
                        x:Name="OkButton" />
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>